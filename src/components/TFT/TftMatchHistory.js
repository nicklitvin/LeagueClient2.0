import Tftheader from "./Tftheader";
import styles from "../../css/TFT/TftMatchHistory.module.css";

export default function TftMatchHistory() {
    return (
        <div className={styles.pageContainer}>
            {Tftheader(1)}
        </div>
    )
}